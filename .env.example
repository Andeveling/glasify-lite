# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Vercel Environment Variables
NEXT_PUBLIC_VERCEL_ENV="development"
NEXT_PUBLIC_VERCEL_URL="localhost:3000"
NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL="glasify-lite.vercel.app"

# Next Auth
# You can generate a new secret on the command line with:
# npx auth secret
# https://next-auth.js.org/configuration/options#secret
AUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST=true

# Next Auth Google Provider
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# Admin User (Optional)
# Email address of the admin user. This user will have access to the dashboard.
# If not set, all users will be redirected to /my-quotes after sign in.
ADMIN_EMAIL=""

# Prisma
# https://www.prisma.io/docs/reference/database-reference/connection-urls#env
# For Vercel deployment, use connection pooling
DATABASE_URL="postgresql://postgres:password@localhost:5432/glasify-litle"
DIRECT_URL="postgresql://postgres:password@localhost:5432/glasify-litle"

# Email Service (Resend recommended for Vercel)
RESEND_API_KEY=""
FROM_EMAIL="noreply@glasify-lite.vercel.app"

# Analytics (Optional)
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=""

# Feature Flags (Optional)
NEXT_PUBLIC_ENABLE_VERCEL_TOOLBAR="true"

# ============================================================
# EXPORT CONFIGURATION (Quote PDF/Excel Generation)
# ============================================================

# Company name displayed in exported documents (quotes, invoices)
# If not set, falls back to TENANT_BUSINESS_NAME
NEXT_PUBLIC_COMPANY_NAME=""

# Company logo URL for PDF/Excel headers
# Supports: HTTPS URLs, data URIs, or relative paths from /public
# Examples: "https://cdn.example.com/logo.png", "/logo.png"
# Recommended: PNG or SVG, max 200KB, transparent background
NEXT_PUBLIC_COMPANY_LOGO_URL=""

# PDF page size for quote exports
# Options: "A4" (210x297mm), "LETTER" (8.5x11in), "LEGAL" (8.5x14in)
# Default: "A4" (international standard)
EXPORT_PDF_PAGE_SIZE="A4"

# Maximum items per quote export (prevents oversized documents)
# Typical values: 50-200 items
# Default: 100 items
EXPORT_MAX_ITEMS="100"

# ============================================================
# TENANT CONFIGURATION (Required for Seed Scripts)
# ============================================================
# These variables configure the singleton TenantConfig record (id: "1")
# Used by prisma/seed-tenant.ts to initialize your business configuration
# Validation: src/env-seed.ts (using @t3-oss/env-nextjs + Zod)
#
# HOW IT WORKS:
# - TenantConfig is a SINGLETON pattern (only ONE record exists)
# - The ID is always "1" (enforced by schema default)
# - Seed script uses upsert: creates if missing, updates if exists
# - Safe to run multiple times (idempotent)
#
# IMPORTANT: All TENANT_* variables are validated before seeding
# If any required variable is missing or invalid, seed will fail with clear error
# ============================================================

# Business name that appears in quotes, invoices, and reports
# Examples: "Glasify Solutions S.A.S.", "Acme Glass Corp", "Vidrios Premium Ltda"
TENANT_BUSINESS_NAME="Your Business Name"

# ISO 4217 currency code (exactly 3 uppercase letters)
# Common values:
#   COP = Colombian Peso
#   USD = US Dollar
#   EUR = Euro
#   MXN = Mexican Peso
#   ARS = Argentine Peso
#   CLP = Chilean Peso
# See: https://en.wikipedia.org/wiki/ISO_4217
TENANT_CURRENCY="COP"

# IETF BCP 47 locale for formatting dates, numbers, and currency
# Format: language-COUNTRY (lowercase-UPPERCASE)
# Examples:
#   es-CO = Spanish (Colombia) → "1.234,56 COP"
#   en-US = English (US) → "$1,234.56"
#   es-MX = Spanish (Mexico) → "$1,234.56 MXN"
#   es-AR = Spanish (Argentina) → "$ 1.234,56 ARS"
#   es-CL = Spanish (Chile) → "$ 1.234,56"
# See: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl#locales_argument
TENANT_LOCALE="es-CO"

# IANA timezone identifier for date/time handling
# Used for: quote expiration, timestamps, scheduling
# Examples:
#   America/Bogota (UTC-5, Colombia)
#   America/New_York (UTC-5/-4, US Eastern)
#   America/Mexico_City (UTC-6, Mexico)
#   America/Argentina/Buenos_Aires (UTC-3, Argentina)
#   America/Santiago (UTC-4/-3, Chile)
#   Europe/Madrid (UTC+1/+2, Spain)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TENANT_TIMEZONE="America/Bogota"

# Number of days a quote remains valid before expiring
# Typical values: 7, 15, 30, 45
# Used to calculate: validUntil = createdAt + quoteValidityDays
TENANT_QUOTE_VALIDITY_DAYS="15"

# ============================================================
# TENANT CONTACT INFO (Optional)
# ============================================================
# These fields are optional but recommended for quotes/invoices
# Leave empty or omit if not needed
# ============================================================

# Business contact email (must be valid email format)
# Examples: "ventas@glasify.com", "cotizaciones@acme.co"
TENANT_CONTACT_EMAIL=""

# Business contact phone (any format accepted)
# Examples: "+57 300 123 4567", "(601) 234-5678", "300-123-4567"
TENANT_CONTACT_PHONE=""

# Business physical address (free text)
# Examples: "Calle 123 #45-67, Bogotá, Colombia", "1234 Main St, Suite 100, New York, NY 10001"
TENANT_BUSINESS_ADDRESS=""

