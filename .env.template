# ============================================================
# ENVIRONMENT VARIABLES - TEMPLATE
# ============================================================
# This is a TEMPLATE FILE - DO NOT put real secrets here
# Copy this file to .env or .env.local and fill in your values
#
# File hierarchy:
#   .env.example     → Template (committed, no secrets)
#   .env             → Base config (gitignored, can have non-sensitive defaults)
#   .env.local       → Local overrides (gitignored, highest priority)
#   .env.production  → Production config (Vercel only, never local)
#
# For production: Set variables in Vercel Dashboard → Settings → Environment Variables
# ============================================================

# ============================================================
# AUTHENTICATION (Better Auth)
# ============================================================
# Generate secret with: npx auth secret
# DO NOT commit real secrets!
BETTER_AUTH_SECRET="generate-with-npx-auth-secret"

# Base URL for OAuth callbacks
BASE_URL="http://localhost:3000"

# Google OAuth (get from: https://console.cloud.google.com/)
AUTH_GOOGLE_ID="your-google-client-id.apps.googleusercontent.com"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# Admin user email (optional - grants dashboard access)
# Leave empty to disable admin role
ADMIN_EMAIL=""

# ============================================================
# DATABASE (PostgreSQL via Neon)
# ============================================================
# DEVELOPMENT: Point to local PostgreSQL or Neon dev branch
# PRODUCTION: Use Neon pooled connection (-pooler suffix)
#
# Get from Neon Dashboard → Connect → Connection String
DATABASE_URL="postgresql://user:password@localhost:5432/glasify"

# Direct connection for migrations (without -pooler)
# Only needed if using Neon with connection pooling
# DIRECT_URL="postgresql://user:password@host.region.aws.neon.tech/db"

# Optional: Limit connections during build (Neon Free Tier)
# Recommended: "1" for Free Tier, "5" for Pro, omit for unlimited
# PRISMA_CONNECTION_LIMIT="1"

# ============================================================
# BRANDING (Client-Side - Public)
# ============================================================
# These are INLINED at build time (safe to expose to browser)

# Company name displayed in UI
NEXT_PUBLIC_COMPANY_NAME="Your Company Name"

# Company logo URL (relative path from /public or absolute URL)
# Examples: "/logo.png", "https://cdn.example.com/logo.svg"
NEXT_PUBLIC_COMPANY_LOGO_URL=""

# ============================================================
# TENANT CONFIGURATION (For Seed Scripts Only)
# ============================================================
# Required by: pnpm seed (prisma/seed-tenant.ts)
# Creates the TenantConfig singleton record in database
# NOT required for app runtime (only for initial setup)
#
# Run seed after setting these: pnpm seed:minimal
# ============================================================

# Business name (appears in quotes, invoices, reports)
TENANT_BUSINESS_NAME="Your Business Name S.A."

# ISO 4217 currency code (3 uppercase letters)
# Examples: COP (Colombia), USD (USA/Panama), EUR (Europe), MXN (Mexico)
TENANT_CURRENCY="USD"

# IETF BCP 47 locale for date/number formatting
# Examples: es-CO (Colombia), es-PA (Panama), en-US (USA), es-MX (Mexico)
TENANT_LOCALE="es-CO"

# IANA timezone identifier
# Examples: America/Bogota, America/Panama, America/Mexico_City
TENANT_TIMEZONE="America/Bogota"

# Quote validity in days (how long quotes remain valid)
TENANT_QUOTE_VALIDITY_DAYS="15"

# ============================================================
# TENANT CONTACT INFO (Optional - For Seed)
# ============================================================
# These appear in quotes and invoices (optional fields)
TENANT_CONTACT_EMAIL="ventas@yourcompany.com"
TENANT_CONTACT_PHONE="+1 234 567 8900"
TENANT_BUSINESS_ADDRESS="123 Main St, City, Country"

# ============================================================
# NODE ENVIRONMENT (Auto-set by Next.js)
# ============================================================
# Auto-detected by Next.js:
#   next dev   → development
#   next build → production
# Only override for testing purposes
# NODE_ENV="development"

# ============================================================
# VERCEL (Auto-populated in Vercel, manual in local)
# ============================================================
NEXT_PUBLIC_VERCEL_ENV="development"
NEXT_PUBLIC_VERCEL_URL="localhost:3000"
NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL="your-app.vercel.app"
