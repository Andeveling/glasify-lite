# ============================================================
# PRODUCTION ENVIRONMENT VARIABLES (Vercel)
# ============================================================
# This is a TEMPLATE for production environment variables
# Copy relevant variables to Vercel Dashboard → Settings → Environment Variables
#
# IMPORTANT:
# - DO NOT commit .env.production.local to git (contains secrets)
# - Configure ALL variables in Vercel Dashboard before deploy
# - Use Vercel CLI: `vercel env pull .env.production.local` to sync
# ============================================================

# ============================================================
# DATABASE (Neon PostgreSQL - Production)
# ============================================================
# Get from Neon Console → Project → Dashboard → Connect

# POOLED CONNECTION (for queries - Server Components, API Routes, tRPC)
# Format: postgresql://[user]:[password]@[hostname]-pooler.region.aws.neon.tech/[dbname]?sslmode=require
DATABASE_URL="postgresql://user:password@hostname-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require"

# DIRECT CONNECTION (for migrations - prisma migrate deploy)
# Format: postgresql://[user]:[password]@[hostname].region.aws.neon.tech/[dbname]?sslmode=require
DIRECT_URL="postgresql://user:password@hostname.us-east-2.aws.neon.tech/neondb?sslmode=require"

# ============================================================
# AUTHENTICATION (Better Auth)
# ============================================================
# CRITICAL: Generate new secret for production with: npx auth secret
# NEVER reuse development secret in production
BETTER_AUTH_SECRET="[GENERATE_NEW_SECRET_WITH_npx_auth_secret]"

# Production URL (auto-populated by Vercel, but can override)
BASE_URL="https://glasify-lite.vercel.app"

# ============================================================
# OAUTH PROVIDERS (Google)
# ============================================================
# PRODUCTION OAuth Credentials from Google Cloud Console
# Create SEPARATE credentials for production (different redirect URIs)
# Redirect URI: https://glasify-lite.vercel.app/api/auth/callback/google
AUTH_GOOGLE_ID="[PRODUCTION_GOOGLE_CLIENT_ID]"
AUTH_GOOGLE_SECRET="[PRODUCTION_GOOGLE_CLIENT_SECRET]"

# ============================================================
# ADMIN USER
# ============================================================
# Email of the admin user (has access to /admin dashboard)
# IMPORTANT: Use a REAL email that exists in production
ADMIN_EMAIL="admin@vitrorojas.com"

# ============================================================
# NODE ENVIRONMENT
# ============================================================
# Auto-set by Vercel to "production"
# DO NOT override unless you know what you're doing
NODE_ENV="production"

# ============================================================
# BRANDING (Client-Side - Public)
# ============================================================
# Company name displayed in UI
NEXT_PUBLIC_COMPANY_NAME="Vitro Rojas"

# Company logo URL
# Option A: Relative path from /public (committed to repo)
# NEXT_PUBLIC_COMPANY_LOGO_URL="/logo.png"
#
# Option B: CDN URL (recommended for production)
# NEXT_PUBLIC_COMPANY_LOGO_URL="https://cdn.vitrorojas.com/logo.png"
NEXT_PUBLIC_COMPANY_LOGO_URL="https://cdn.vitrorojas.com/logo.png"

# ============================================================
# VERCEL (Auto-populated by Vercel)
# ============================================================
# These are AUTO-SET by Vercel, DO NOT configure manually
# NEXT_PUBLIC_VERCEL_ENV="production"
# NEXT_PUBLIC_VERCEL_URL="glasify-lite.vercel.app"
# NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL="glasify-lite.vercel.app"

# ============================================================
# EMAIL SERVICE (Resend)
# ============================================================
# Get API key from: https://resend.com/api-keys
# IMPORTANT: Use PRODUCTION API key, not test key
RESEND_API_KEY="re_[PRODUCTION_API_KEY]"

# Verified domain email (must be verified in Resend)
FROM_EMAIL="noreply@vitrorojas.com"

# ============================================================
# EXPORT CONFIGURATION (Server-Side)
# ============================================================
EXPORT_PDF_PAGE_SIZE="A4"
EXPORT_MAX_ITEMS="100"

# ============================================================
# TENANT CONFIGURATION (Required for Seed Scripts)
# ============================================================
# Used by: pnpm seed:minimal (run ONCE after first deploy)
# Validation: src/env-seed.ts (Zod schema)

# Business name (appears in quotes, invoices, reports)
TENANT_BUSINESS_NAME="Vitro Rojas Panamá"

# ISO 4217 currency code (3 uppercase letters)
# Panama uses USD (US Dollar)
TENANT_CURRENCY="USD"

# IETF BCP 47 locale (language-COUNTRY)
# es-PA = Spanish (Panama)
TENANT_LOCALE="es-PA"

# IANA timezone identifier
# America/Panama = UTC-5 (no DST)
TENANT_TIMEZONE="America/Panama"

# Quote validity in days
TENANT_QUOTE_VALIDITY_DAYS="15"

# ============================================================
# TENANT CONTACT INFO
# ============================================================
# IMPORTANT: Use REAL production contact information
TENANT_CONTACT_EMAIL="ventas@vitrorojas.com"
TENANT_CONTACT_PHONE="+507 6123-4567"
TENANT_BUSINESS_ADDRESS="Panamá, Panamá"

# ============================================================
# ANALYTICS & MONITORING (Optional)
# ============================================================
# Vercel Analytics (auto-enabled in Vercel Pro)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID="[AUTO_POPULATED_BY_VERCEL]"

# ============================================================
# SECURITY & FEATURES (Optional)
# ============================================================
# Vercel Toolbar (disable in production)
NEXT_PUBLIC_ENABLE_VERCEL_TOOLBAR="false"

# ============================================================
# DEPLOYMENT CHECKLIST
# ============================================================
# Before deploying to production, ensure:
#
# [ ] New BETTER_AUTH_SECRET generated (npx auth secret)
# [ ] Production Google OAuth credentials created
# [ ] Neon project created and connection strings obtained
# [ ] Resend account created and domain verified
# [ ] All environment variables configured in Vercel Dashboard
# [ ] Logo uploaded to CDN (if using NEXT_PUBLIC_COMPANY_LOGO_URL)
# [ ] ADMIN_EMAIL set to real admin email
# [ ] Database migrations applied: DATABASE_URL=$DIRECT_URL pnpm db:migrate
# [ ] Seed executed ONCE: pnpm seed:minimal
# [ ] Test deployment in Preview environment first
# [ ] Verify no .env*.local files committed to git
#
# ============================================================
