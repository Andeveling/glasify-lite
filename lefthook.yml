pre-commit:
  jobs:
    - run: pnpm dlx ultracite fix {staged_files}
      glob:
        - "src/**/*.{js,jsx,ts,tsx,json,jsonc,css}"
        - "*.{js,jsx,ts,tsx,json,jsonc,css,md,mdx}"
      stage_fixed: true

commit-msg:
  jobs:
    - run: pnpm dlx commitlint --edit $1
