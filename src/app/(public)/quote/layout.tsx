// Force dynamic rendering for quote page
export const dynamic = 'force-dynamic';
export const runtime = 'nodejs';

export default function QuoteLayout({ children }: { children: React.ReactNode }) {
  return children;
}
